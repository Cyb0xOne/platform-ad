# Salin ke .env dan isi dengan kredensial Postgres ForcAD.
# Nilai password ada di ~/adlab/forcad/docker_config/postgres_environment.env
# Host = `postgres` (alias service ForcAD di network forcad_default).
FORCAD_DSN=postgresql://forcad:PASSWORD@postgres:5432/forcad
