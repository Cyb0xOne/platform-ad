# SDD ledger — plan: docs/superpowers/plans/2026-07-27-enowars10-checkers.md

Task 1: review — spec ⚠️, 1 Critical (status_for salah klasifikasi requests-error jadi DOWN, harusnya MUMBLE), 1 Minor (leading-slash tak ada test). Fix round 1 dikirim ke implementer (resume a7f0c60c). FIX_BASE=bd516c7.
Task 1: fix round 1/5 (2 addressed, 0 open; commits bd516c7..a71f2ec)
Task 1: complete (commits 4fbe11b..a71f2ec, review clean)
Task 2: dispatched (sonnet, background) — deploy greple + gate + teardown. BASE=fb6b702.
Task 2: review — Approved, 1 Important (c unbound di __main__, warisan template brief).
Task 2: fix round 1/5 (1 addressed, 0 open; commits bd9ea4b..a8ab85d)
Task 2: complete (commits fb6b702..a8ab85d, review clean)
  KEPUTUSAN LINTAS-TASK: (1) _lib di-deploy sekali ke /checkers/_lib (SIBLING, shared semua eno10_*), checker.py sys.path parent.parent/_lib menuju ke sana. (2) Iterasi checker: docker cp ke celery jalan (tanpa rebuild) selama tak ada dep baru. (3) Reachability: jalankan service di VM 10.13.37.x atau host+forcad_default; gate `docker exec celery ... <ip>`. (4) Service up->gate->teardown; config ditunda. (5) __main__ sudah difix di plan (c di luar try) — checker code Task 3-10 sudah benar.
  CATATAN: patch build Zig greple ada di salinan server saja (~/adlab/eno10/greple), tidak di repo. Revisit bila greple masuk roster.
Task 3: dispatched (sonnet, bg) — deploy d3pl0y + gate + teardown. BASE=935d2e6. Risiko: build Hare-VM eksotik.
Task 3: implementer DONE (commit bf26cbe). Gate 101/101/101 + 102 bogus + 104 dead-host. Build Hare mulus, tanpa patch. Temuan bentuk request: endpoint di bawah prefix /api, register pakai form-urlencoded, respons register plain-text token (bukan JSON).
  CATATAN (dibawa ke task berikutnya): deploy/enowars10-deploy.sh bisa menulis docker-compose.override.yml rusak permanen kalau `docker compose config --services` pernah kosong di run pertama (file hanya dibuat saat belum ada). Workaround: hapus override.yml lalu tulis tangan. Belum dipatch (di luar scope Task 3).
Task 3: review — Spec ✅, Task quality Approved. 0 Critical, 0 Important, 2 Minor. Klasifikasi DOWN/CORRUPT/MUMBLE diverifikasi struktural lintas ketiga aksi, bukan cuma dari 5 kasus gate.
Task 3: minor (deferred): checker.py:29 `rand_text(8).replace(" ","")` secara teori bisa jadi nama objek kosong (~4e-14); warisan template, presedennya sama di greple.
Task 3: minor (deferred): checker.py:52 `assert_eq(status,200,...,CORRUPT)` menyamaratakan semua non-200 (401/404/5xx transien) jadi CORRUPT, padahal 5xx transien lebih tepat MUMBLE. Preseden identik greple/checker.py:53 — kalau diperbaiki, perbaiki keduanya.
Task 3: ⚠️ diselesaikan controller: bentuk request (/api prefix, register form-urlencoded, token plain-text) tak bisa diverifikasi dari diff karena sumber d3pl0y di luar repo — TAPI gate live put=101 get=101 mustahil tercapai kalau bentuknya salah. Dianggap terkonfirmasi empiris.
Task 3: ⚠️ CONSTRAINT UNTUK REGISTRASI CONFIG (semua task): d3pl0y punya reaper TTL ~12 menit untuk objek. Artinya round_time x flag_lifetime harus <= ~720 s. Dengan flag_lifetime 5, round_time maksimum ~120 s (=600 s). config.fase1.yml sekarang pakai round_time 180 (=900 s) → GET flag lama akan gagal. Putuskan saat menyusun config.enowars10.yml.
Task 3: complete (commits 935d2e6..bf26cbe, review clean)
Task 4: dispatched (sonnet, bg) — deploy flagdrive + gate + teardown. BASE=bf26cbe. Risiko: build Rust lambat/gagal; auth di-obfuscate di sumber Rust.
Task 4: sempat berhenti menunggu build cargo; di-resume via SendMessage dengan instruksi polling build sampai selesai. Pelajaran untuk task berikutnya: build lama harus di-poll di dalam subagent, jangan berhenti.
Task 4: implementer DONE (commit 315b437). Gate 101/101/101 + 102 + 104. Koreksi vs brief: `visibility` harus "Private" (kapital, varian enum Rust; huruf kecil -> 400). Route, field multipart file/json, dan file_id ternyata sudah benar.
  UTANG TEKNIS BERULANG (2 kejadian sekarang): patch build hanya ada di salinan server, tidak di repo. greple (Zig) di ~/adlab/eno10/greple; flagdrive di ~/adlab/eno10/flagdrive (strip `RUN --mount=type=cache` karena BuildKit tak ada + perbaikan href aset tailwind di frontend/index.html). rsync ulang = build gagal lagi. Pertimbangkan task khusus: simpan patch build ini di repo (deploy/patches/<service>.patch) sebelum branch ditutup.
  flagdrive juga punya reaper ~12 menit seperti d3pl0y — makin menguatkan batas round_time x flag_lifetime <= ~720 s.
Task 4: review — Spec ✅, Task quality Approved. 0 Critical, 0 Important, 3 Minor. Reviewer memverifikasi jalur multipart: flag benar-benar masuk sebagai byte file (bukan di metadata JSON) dan get() memeriksa r.content, jadi flag yang diubah lawan benar-benar terdeteksi.
Task 4: minor (deferred): checker.py:44 `r.json()` dipanggil dua kali + `or` menelan file_id==0 sebelum cek `is not None`. Praktis mustahil (u64), rapikan bila file disentuh lagi.
Task 4: minor (deferred): blok __main__ identik byte-for-byte di greple/d3pl0y/flagdrive. Kandidat refactor lintas-checker (helper A.main(Checker) di _lib) SETELAH semua checker jadi — jangan disisipkan per-task.
Task 4: minor (deferred): sama seperti Task 3, non-200 di get() semua jadi CORRUPT.
Task 4: ⚠️ dicatat (perilaku harness, bukan cacat task): flag_id yang benar-benar bukan JSON -> decode_state ValueError -> status_for -> MUMBLE (103), bukan CORRUPT (102). Tak masalah di produksi (flag_id selalu berasal dari PUT sebelumnya); kasus gate "bogus flag_id" yang benar adalah state JSON valid dengan id tak ada — itu yang dipakai Task 3 & 4.
Task 4: complete (commits bf26cbe..315b437, review clean)
Task 5: dispatched (sonnet, bg) — deploy signmemaybe (.NET, sumber di dir SignMeMaybe/) + gate + teardown. BASE=315b437. Risiko: build .NET lambat; casing JSON camelCase vs PascalCase. checker_timeout: 25 dicatat untuk config nanti.
Task 5: implementer DONE (commit 66779c6). Gate 101/101/101 + 102 + 104 (service di 192.168.0.17:1984). Build .NET mulus, tanpa patch.
  Koreksi besar vs brief: POST /api/contracts TIDAK mengembalikan `id` — identifier publiknya `reference` ("CNTR-<24hex>"). Route GET /api/contracts/{id} TIDAK ADA; jalur baca sebenarnya GET /api/contracts/{reference}/versions/latest, flag ada di field `content`. Header X-Session-Token dan field token sudah benar.
  BUG DEPLOY SCRIPT TERKONFIRMASI (bukan lagi teoretis): deploy/enowars10-deploy.sh benar-benar menulis override null `services:` saat `docker compose config --services` kosong di run pertama, dan semua panggilan compose gagal sesudahnya. Di-workaround manual. NAIKKAN jadi task wajib sebelum branch ditutup.
  Catatan keamanan (bukan urusan checker): GET /api/contracts/{reference}/versions/latest tidak memeriksa kepemilikan kontrak — kemungkinan besar itu vuln yang disengaja. Checker sengaja tidak bergantung padanya.
Task 5: review — Spec ✅, Task quality Approved. 0 Critical, 0 Important, 3 Minor. Reviewer memverifikasi ulang klaim "brief salah" langsung ke sumber C# (ContractEndpoints.cs) dan semuanya cocok baris per baris.
Task 5: minor (deferred): checker.py:47-48 `ref` dicek `is not None` tapi tidak dicek non-kosong. Tak terjangkau di praktik (reference selalu CNTR-<24hex>); pola sama di flagdrive.
Task 5: minor (deferred): flag_id bukan-JSON -> MUMBLE, bukan CORRUPT. Warisan harness, sama di semua checker.
Task 5: ⚠️ TEMUAN LINTAS-TASK ttg BENTUK GATE (untuk final review): gate 5-kasus yang diwajibkan plan TIDAK menguji (a) MUMBLE saat service hidup-tapi-salah, dan (b) CORRUPT saat flag masih ada tapi DIUBAH. Keduanya diverifikasi lewat pembacaan kode di Task 3/4/5, bukan empiris. Kasus (b) yang paling penting di permainan nyata (lawan menimpa flag). Pertimbangkan menambah kasus gate ke-6 (PUT, ubah/timpa konten, GET -> harus 102) untuk service yang memungkinkan.
Task 5: complete (commits 315b437..66779c6, review clean)
Task 6: dispatched (sonnet, bg) — deploy overeats (stack 5 container, sumber di dir OverEats/) + gate + teardown. BASE=66779c6. Risiko: stack terberat sejauh ini utk RAM server; endpoint pembawa flag memang belum pasti di brief. Diminta melaporkan interval container `cleanup` (feed keputusan round timing).
Task 6: implementer DONE (commit 2bc1cb0). Gate 101/101/101 + 102 + 104. Build bersih, tanpa patch.
  DEVIASI TOPOLOGI (masuk akal, bukan cacat): di-gate terhadap VM tim 10.13.37.11, bukan host server, karena forcad-postgres-1 sudah mengikat port 5432 di host dan checker mematok PORT=5432 (port nginx eksternal OverEats). Di permainan nyata service memang jalan di vulnbox, jadi bentrok ini tidak terulang.
  Reaper overeats = 12 menit (cleanup_old_data() di init.sql) — konsisten dgn d3pl0y & flagdrive. Sekarang 3 service mengonfirmasi batas yg sama: round_time x flag_lifetime <= ~720 s.
  Usulan checker_timeout: 30 (put() melakukan 7 round-trip HTTP berurutan, terbanyak sejauh ini).
  Catatan kebersihan: ada skrip verifikasi manual tertinggal di layer writable container celery, tak bisa dihapus (permission). Tak berbahaya; bersihkan saat rebuild image celery berikutnya.
Task 6: review — Spec ✅, Task quality Approved. 0 Critical, 0 Important, 4 Minor. Ketiga risiko yang saya minta dicek beres semua terhadap sumber primer:
  (1) Peringatan pyright di checker.py:105 = false positive TERBUKTI. checklib 0.7.0 `assert_` -> `cquit` -> selalu `raise CheckFinished`, jadi baris sesudah assert gagal tak pernah tercapai. Berlaku untuk SEMUA checker, bukan cuma overeats — peringatan pyright serupa di file lain juga bisa diabaikan.
  (2) Ukuran file ~115 baris memang layak: place_order mensyaratkan role "customer" DAN restaurant_id yang sudah ada, sedangkan init.sql tidak menyemai restoran. Jadi 2 akun (customer + restaurant) itu keharusan service, bukan kemewahan.
  (3) Gate di VM sah: nginx OverEats `ports: 5432:80` — 5432 hanya publish di host, tak ada yang listen 5432 di dalam container. Pola Task 2-5 (attach ke forcad_default lalu tembak container-ip:PORT) justru MUSTAHIL untuk service ini.
  Reviewer juga memverifikasi jalur enkripsi: note di-AES-CBC + HMAC saat disimpan; kalau ciphertext ditimpa, HMAC gagal dan note hilang dari list -> checker benar mengembalikan CORRUPT. Bukti round-trip jujur.
Task 6: minor (deferred): checker.py:38,54 token diambil `.json().get("token")` langsung tanpa cek status dulu. Kalau login balas non-JSON, error-nya kehilangan pesan spesifik (status akhir tetap MUMBLE). Pola sama di semua sibling.
Task 6: minor (deferred): blok register+login customer/restaurant nyaris kembar (checker.py:36-39 vs 51-54).
Task 6: ⚠️ DISELESAIKAN CONTROLLER: reviewer minta konfirmasi apakah "flag_id malformed -> MUMBLE, bukan CORRUPT" sudah diterima sadar. YA — sudah diputuskan di Task 4 (lihat entri Task 4 ⚠️ di atas): di produksi flag_id selalu berasal dari PUT sebelumnya, jadi jalur itu tak terjadi. Bukan utang yang menggantung.
Task 6: complete (commits 66779c6..2bc1cb0, review clean)
Task 7: implementer DONE (commit c68ff1f, 172 baris — file terbesar sejauh ini). Gate 101/101/101 + 102 + 104 di forcad_default (192.168.0.17:1337), plus satu ronde put/get kedua yg juga 101/101. Build Gleam BERHASIL (tidak BLOCKED).
  Koreksi vs brief: request form-urlencoded (bukan JSON) karena `wisp.require_form`; respons sukses = 303 redirect dengan public_id di header Location (bukan field JSON `id`).
  TEMUAN PALING PENTING & LINTAS-SERVICE: wisp menandai cookie sesi `Secure` untuk host apa pun kecuali literal localhost/127.0.0.1. Akibatnya requests.Session di atas http:// TIDAK PERNAH mengirim balik cookie itu. Ini kena di SEMUA jalur ForcAD nyata (IP tim, tak pernah localhost), bukan cuma di jaringan uji. Diatasi dengan _sync_cookie_header() yang menyalin nilai cookie ke header Cookie manual. Kalau ada service wisp/Gleam lain, jebakan yang sama berlaku.
  UTANG TEKNIS PATCH SERVER — KEJADIAN KE-3: dependency git `bravo` tak terjangkau via github.com dari sandbox build, tapi codeload.github.com bisa. Di-vendor lewat tarball commit terpin, lalu gleam.toml/manifest.toml/Dockerfile diarahkan ke path lokal. Hanya ada di salinan server. (greple/Zig, flagdrive/BuildKit+tailwind, funsplash/bravo).
Task 7: review — Spec ✅ TAPI Task quality NEEDS FIXES. 1 Important, 2 Minor.
  IMPORTANT: checker.py:116 & :148 `s.get("/napi/me")` tanpa allow_redirects=False (padahal :111/:123/:140 pakai). Sesi anonim -> 303 -> requests ikut redirect -> mendarat di index fallback yg SELALU 200 -> `assert_eq(me.status_code, 200)` jadi assertion MATI. Sinyal gagal sebenarnya baru muncul di baris berikutnya sbg JSONDecodeError yg jatuh ke except generik, jadi pesan diagnostik yg sengaja ditulis hilang. Status akhir tetap MUMBLE (benar), tapi assertion tak melakukan apa yg diklaim komentarnya.
  Reviewer memverifikasi klaim cookie Secure sampai ke sumber wisp 2.2.2 + gleam_http DAN membangun server repro lokal. Juga membuktikan "solusi buku teks" (DefaultCookiePolicy secure_protocols) TIDAK berpengaruh di requests, karena Session.prepare_request membungkus ulang cookie ke jar baru dan membuang policy kustom. Jadi header manual memang ~fix minimal, bukan workaround serampangan.
Task 7: minor (deferred): checker.py:1-90 rasio komentar 62% (sibling 34-49%), banyak menduplikasi prosa task-7-report.md.
Task 7: minor (deferred): checker.py:67-68 fallback `.get("location","")` = dead code, header requests sudah case-insensitive.
Task 7: fix round 1/5 dikirim (resume implementer a2675709). FIX_BASE=c68ff1f. Scope: 2x allow_redirects=False + gate ulang 5 kasus.
Task 7: fix round 1/5 SELESAI (commit af74796, tepat 2 baris). Gate ulang 101/101/101 + 102 + 104, tanpa regresi. Implementer membuktikan maksud fix lewat A/B: salinan scratch dgn _sync_cookie_header dimatikan -> sebelum fix `JSONDecodeError` mentah, sesudah fix pesan "join gagal (sesi tak valid)" + assertion `303 != 200` bersih. Keduanya tetap 103, jadi memang bug diagnostik, bukan bug hasil.
Task 7: re-review terbatas — ✅ Addressed, 0 isu baru, Fix quality Approved. Jalur sukses tak terpengaruh (sukses /napi/me balas 200 langsung, tak pernah redirect).
Task 7: complete (commits 2bc1cb0..af74796, review clean setelah 1 fix round)
Task 8: implementer DONE (commit bc05406, 140 baris). Gate 101/101/101 + 102 + 104 di VM 10.13.37.12, plus ronde put/get kedua 101/101.
  Koreksi vs brief: register butuh field `display_name` (400 tanpa itu); register SUDAH auto-login (bikin sesi + set cookie), jadi panggilan POST /api/login terpisah dibuang; GET /api/users/{handle} terkonfirmasi publik (di luar grup authed) sehingga get() tak perlu login dan state cukup simpan handle; PATCH /api/me {"bio": flag} benar dan responsnya query DB ulang, bukan echo.
  Dua jebakan Task 7 tidak kena di sini (COOKIE_SECURE=0 di compose deployment ini, diperiksa lewat header Set-Cookie), tapi allow_redirects=False tetap dipasang defensif.
  Topologi: web publish 6789:8080 (host-publish saja) -> jalan di VM 10.13.37.12, pola Task 6.
  PENEMUAN INFRA BERGUNA: Docker di VM tim PUNYA BuildKit yang berfungsi, tidak seperti host server. Ini build bersih pertama dari 8 task. Untuk service eksotik berikutnya, membangun di VM bisa menghindari patch strip `RUN --mount=type=cache`.
  Reaper leet-date = 15 menit (container cleanup menghapus baris `users`, jadi flag ikut hilang). Sedikit lebih longgar dari 12 menit trio d3pl0y/flagdrive/overeats, tapi batas efektif tetap yang paling ketat.
  Usulan checker_timeout: 15.
Task 8: review — Spec ✅, Task quality Approved. 0 Critical, 0 Important, 1 Minor. Reviewer mengecek SEMUA sitasi sumber di komentar checker (router.go, auth.go, profiles.go, sessions.go, config.go, health.go, nginx.conf, docker-compose.yml) sampai nomor barisnya — nol ketidakcocokan.
  Pembuangan POST /api/login TERBUKTI aman: Register dan Login memanggil urutan CreateSession+SetSessionCookie yang identik (auth.go:85-90 vs :126-131), dan kalau suatu saat register berhenti auto-login, RequireAuth akan 401 -> assert di checker.py:128 gagal keras sebagai MUMBLE. Tidak ada jalur lolos diam-diam.
  Reviewer juga memverifikasi keaslian bukti gate: format flag_id yg dilaporkan cocok persis dgn json.dumps separators=(",",":") milik encode_state, dan pesan CORRUPT cocok verbatim dgn checker.py:140. Bukan parafrase.
Task 8: minor (deferred): asumsi COOKIE_SECURE=0 berasal dari compose di salinan repo ini. Kalau vulnbox pertandingan menyetel COOKIE_SECURE=1 di atas http polos, checker akan MUMBLE keras (bukan salah skor) — risikonya ketersediaan, bukan kebenaran. Cek nilai itu di compose vulnbox sebenarnya sebelum mendaftarkan service.
Task 8: CATATAN: checkers/requirements.txt TIDAK ADA di repo ini (adanya di sisi server, di direktori checkers ForcAD). Constraint "ditambah jangan ditimpa" tetap berlaku untuk berkas di server.
Task 8: complete (commits af74796..bc05406, review clean)
Task 9: implementer DONE (commit be13dc8, 200 baris — terbesar). Gate 101/101/101 + 102 + 104 di forcad_default (192.168.0.17:6767).
  Brief ternyata salah di banyak titik yg hanya terungkap dari transkrip hidup: Register butuh 3 prompt (username/password/ALAMAT RUMAH, bukan 2); Login 2 prompt terpisah, bukan satu baris; perintah buat kendaraan = `add` (bukan AddVehicle) dan TIDAK PERNAH mengembalikan id, jadi id ditemukan via `search <make-unik>` sesudahnya.
  TEMUAN STRUKTURAL: tidak ada entitas "note" terpisah di skema. `ReadNote <vid> <vid2>` sebenarnya = "otorisasi lewat vehicle id milikmu, lalu baca maintenance_note milik id mana pun yg kau sebut" — itu IDOR yang disengaja (vuln-nya). Checker mengirim id-nya sendiri untuk KEDUA argumen, jadi tak pernah menyentuh vuln. Penting: kalau checker bergantung pada vuln, dia rusak begitu tim menambalnya.
  LineClient TERBUKTI MEMADAI — sendline/recv_until/close saja, tanpa recv ukuran tetap. Timeout default 10 s benar memunculkan DOWN untuk host tak terjangkau maupun koneksi ditolak.
  Reaper: TIDAK ADA yang berbasis waktu. Yang ada prune berbasis kapasitas (simpan 10.000 terbaru). Beda struktur dari 4 service ber-TTL 12-15 menit. Interval timer belum diamati langsung — gap yang diakui jujur.
Task 9: review — Spec ✅, Task quality Approved. 0 Critical, 0 Important, 3 Minor. Kelima risiko bernama terjawab dari sumber primer.
  IDOR TERVERIFIKASI dari `strings bin/SuperRegister`: urutan literal "SELECT id FROM vehicles WHERE id = ? AND user_id = ?;" -> "Authorization failed for your vehicle." -> "SELECT maintenance_note FROM vehicles WHERE id = ?;" (query kedua TANPA filter user_id). Checker kirim id sendiri utk kedua argumen -> tidak bergantung pada vuln.
  Kebocoran socket: TIDAK ADA. checklib assert_* -> cquit -> RAISE CheckFinished (bukan sys.exit langsung), jadi selalu lewat `finally: c.close()` dulu. Berlaku umum untuk semua checker.
  FAKTA LINTAS-TASK YANG DISELESAIKAN — FORMAT FLAG ForcAD = `[A-Z0-9]{31}=` (sumber di repo: deploy/attacker/run_attack.py:28-30 dan adapters/eno/eno_adapter.py:129). Tidak mungkin mengandung koma/spasi. Jadi kekhawatiran "flag merusak baris CSV" DITUTUP, dan ini berlaku untuk checker mana pun yg menyisipkan flag ke format berdelimiter.
Task 9: minor (deferred): checker.py:73-77 P_USERNAME adalah suffix byte-per-byte dari P_USERNAME_CHOOSE (idem password). Aman sekarang krn urutan temporal ketat, tapi bergantung pada kebetulan, bukan jaminan struktural.
Task 9: minor (deferred): laporan meninggalkan kekhawatiran koma/CSV sbg terbuka padahal bisa ditutup dari repo sendiri. Sudah saya tutup di ledger (lihat di atas).
Task 9: minor (deferred): jalur login-gagal->MUMBLE di get() diuji di fase lokal pra-attach, bukan di dalam gate celery resmi.
Task 9: complete (commits bc05406..be13dc8, review clean)
Task 10: dispatched (sonnet, bg) — deploy inbox (SMTP :4321 + IMAP :1234, Go kustom) + gate + teardown. BASE=be13dc8.
  CACAT DI TEMPLATE BRIEF yang saya perintahkan JANGAN diteruskan: check() di brief membungkus self.assert_in di dalam `try/except Exception -> cquit(status_for(e))`. checklib memberi sinyal gagal dengan MELEMPAR CheckFinished, yang juga subclass Exception — jadi banner salah akan tertelan except itu dan jadi ERROR (110), bukan MUMBLE (103). Ini cacat nyata di plan, bukan gaya penulisan.
  Juga diminta: check() harus membuktikan KEDUA protokol hidup (brief cuma cek IMAP); get() menargetkan pesan spesifik (brief memindai seluruh mailbox — jaminan lebih lemah); pastikan kedua port terjangkau dari celery SEBELUM iterasi protokol.
Task 10: implementer DONE (commit 083bc54, file terbesar dalam set). Gate 101/101/101 + 102 + 104 di forcad_default (192.168.0.17, port 1234 & 4321).
  Akun butuh IMAP `REGISTER <user> <pass>` eksplisit; LOGIN tidak pernah auto-create. Password salah dan user tak ada mengembalikan pesan IDENTIK byte-per-byte — itu sebabnya kasus CORRUPT memakai kredensial ASLI + subject yang tak pernah dikirim, bukan kredensial ngawur (kredensial ngawur akan menguji MUMBLE, bukan CORRUPT).
  stdlib smtplib/imaplib jalan untuk semua operasi standar; LineClient dipakai sempit HANYA untuk verb non-standar REGISTER.
  IMAP APPEND RUSAK di server ini (bug framing literal bikin koneksi desync) -> put() mengirim lewat SMTP DATA.
  JEBAKAN LINTAS-CHECKER: `smtplib.SMTPException` diam-diam SUBCLASS `OSError`. Karena status_for punya cabang `isinstance(exc, OSError) -> DOWN`, SMTPException yang tak tertangkap akan salah jadi DOWN padahal seharusnya MUMBLE. Ditangani dgn menangkap tipe itu secara sempit dan menerjemahkan eksplisit.
  Expiry: cron tiap 3 menit menghapus direktori user lebih tua dari 12 menit; login/fetch biasa TIDAK menyegarkan mtime, jadi efektifnya TTL keras ~12-15 menit sejak put(). (Dibaca dari skrip shell, belum diamati satu siklus penuh.)
Task 10: review — Spec ✅ TAPI Task quality NEEDS FIXES. 1 Important, 3 Minor.
  IMPORTANT: checker.py:238 `body = fdat[0][1] or b""` bisa melempar TypeError tak tertangkap -> ERROR (110), padahal kondisinya adalah "flag hilang" yang seharusnya CORRUPT. Mekanisme terkonfirmasi: imaplib mengembalikan (typ="OK", [None]) kalau baris untagged FETCH tak pernah datang; sisi server FetchMessages me-list ulang mailbox dan bisa balik (nil, nil) tanpa error. Terjangkau lewat celah non-atomik `rm -rf` reaper: file .eml sudah hilang tapi direktori mailbox masih ada. Anehnya hasil SEARCH dua baris sebelumnya SUDAH dijaga pola yang sama (`if dat and dat[0]`).
  Reviewer memverifikasi: CheckFinished adalah subclass langsung Exception, bersaudara dgn imaplib.IMAP4.error dan smtplib.SMTPException — jadi tak satu pun `except` sempit di file ini bisa menelan klasifikasi assert. Cacat template brief benar-benar tidak diteruskan.
  Round-trip SMTP->IMAP TERBUKTI jujur dari sumber: cmdData memanggil db.AppendMessage yang SAMA dengan jalur IMAP APPEND, sinkron sebelum balas 250 OK. Bukan artefak sesi yang sama.
Task 10: minor (deferred): jalur CORRUPT "pesan ada tapi isi flag beda" (checker.py:242) tak pernah diuji gate — hanya jalur "pesan tak ditemukan" (:234). Ini instansi konkret dari temuan lintas-task Task 5 soal bentuk gate.
Task 10: minor (deferred): blok komentar 108 baris sebelum kode apa pun.
Task 10: minor (deferred): `.lower()` pada rand_username dibuang tanpa komentar (terbukti aman, isValidUsername mengizinkan huruf campur).
Task 10: fix round 1/5 dikirim (resume implementer a8e51a5f). FIX_BASE=083bc54. Scope: 1 baris + gate ulang + reproduksi 110->102.
Task 10: fix round 1/5 SELESAI (commit 9021649, tepat 1 baris: `body = fdat[0][1] if fdat[0] else b""`). Gate ulang 101/101/101 + 102 + 104, tanpa regresi.
  BUKTI EMPIRIS 110->102 DIDAPAT: FETCH imaplib mentah terhadap .eml yang sengaja dihapus (direktori dibiarkan) memang mengembalikan ('OK', [None]). Checker pra-fix keluar 110 dgn TypeError; checker pasca-fix keluar 102 lewat assert_in yang wajar.
  Catatan metodologis penting: reproduksi naif (hapus file lalu jalankan get() dari awal) TIDAK memicu bug — dia mendarat di jalur CORRUPT "SEARCH kosong" karena SEARCH milik checker juga me-list ulang setelah penghapusan. Jendelanya hanya muncul kalau SEARCH dan FETCH milik checker MENGAPIT penghapusan, jadi butuh hook sleep di salinan scratch (/tmp, tidak pernah di repo).
Task 10: re-review terbatas — ✅ Addressed, 0 isu baru, Fix quality Approved. Jalur sukses tak terpengaruh (tuple FETCH asli selalu truthy). Reviewer juga memverifikasi tak ada berkas scratch bocor ke repo.
Task 10: complete (commits be13dc8..9021649, review clean setelah 1 fix round)

=== HANDOFF (2026-07-27) — sesi pindah komputer ===
UTANG #1 DITUTUP: patch build 3 service sudah diekstrak dari server ke repo di deploy/patches/{greple,flagdrive,funsplash}.patch + README.md.
  Ditemukan saat ekstraksi: greple ternyata juga mem-patch src/utils.zig (API std.fs.cwd().writeFile Zig berubah + rename var `hash`), BUKAN cuma Dockerfile. Ledger sebelumnya hanya menulis "patch build Zig" — kurang lengkap.
  funsplash butuh langkah manual tambahan: vendor/bravo (220 KB) tidak ikut di patch; ambil ulang tarball codeload commit d87b8a4a04d6d370bb54ed6c512a907aa1e43fe9 (perintah ada di deploy/patches/README.md).
ARSIP: .superpowers/sdd/ ini GITIGNORED. Ledger + semua brief/report sudah disalin ke docs/superpowers/reports/enowars10-checkers/ supaya ikut git ke komputer lain.
Dokumen handoff utama: docs/HANDOFF-enowars10-checkers.md

Task 11 CATATAN PENTING (sebelum dispatch): brief Task 11 menyebut `adlab_eno.expect_session` (pexpect). FUNGSI ITU TIDAK ADA. Harness cuma punya: rand_username, rand_password, rand_text, encode_state, decode_state, http, LineClient, status_for. Jadi Task 11 harus pakai LineClient (socket mentah, tangani negosiasi telnet IAC sendiri) — pexpect hanya kalau benar2 perlu, dan itu berarti tambah requirements.txt + blok CUSTOMIZE Dockerfile.fast + rebuild image celery (berdampak ke semua checker), plus pexpect biasanya butuh biner `telnet` yang mungkin tak ada di container celery.
Task 9: dispatched (sonnet, bg) — deploy superregister (protokol CLI TCP :6767) + gate + teardown. BASE=bc05406. CHECKER TCP PERTAMA: LineClient di _lib belum pernah dipakai sungguhan. Skeleton brief sengaja tidak lengkap (urutan prompt AddVehicle harus ditemukan dari interaksi nyata). Dibekali 5 jebakan protokol baris: recv(N) ukuran tetap rapuh, service menggantung jangan tampak sehat, check() harus benar2 membuktikan hidup, regex _parse_ids cuma tebakan pra-lihat-output, tutup socket di semua jalur. DILARANG mengubah _lib/adlab_eno.py (dipakai 8 checker yg sudah lolos).
Task 8: dispatched (sonnet, bg) — deploy leet-date (Go, stack 6 container) + gate + teardown. BASE=af74796. Diminta menyelesaikan 2 hal: (a) apakah baca profil butuh auth (brief membaca anonim & state cuma simpan handle+password), (b) konfirmasi route/field bio. Dibekali 2 jebakan dari Task 7: cookie Secure di atas http, dan redirect membuat assert status berbohong.
Task 7: dispatched (sonnet, bg) — deploy funsplash (Gleam + Postgres 18) + gate + teardown. BASE=2bc1cb0. Risiko TERBESAR sejauh ini: build Gleam/BEAM eksotik; brief sendiri ragu antara /napi/upload vs /napi/collections dan id vs public_id. BLOCKED = hasil yang dapat diterima kalau service-nya yang tak bisa dibangun. Diminta: parse JSON alih-alih regex brief, dan assert login berhasil di get() (login gagal = MUMBLE, bukan CORRUPT).
